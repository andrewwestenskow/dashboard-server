version: '3'

name: 'pgadmin'

networks:
  dashbaord-network:
    name: dashbaord_dashbaord-network
    external: true

volumes:
  pgadmin-storage:

services:
  pgadmin:
    container_name: pgadmin
    image: dpage/pgadmin4
    restart: always
    networks:
      - dashbaord-network
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: root
    volumes:
      - pgadmin-storage:/var/lib/pgadmin
    ports:
      - '5050:80'